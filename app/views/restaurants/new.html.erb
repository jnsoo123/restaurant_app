<div class="container" style="margin-top: 20px;">
  <% if user_signed_in? %>
    <div class="jumbotron">
      <div class="row">
        <div class="col-md-6">
          <h2 class="text-center"><%= t('.enterrestoinfo')%></h2>
		  <p>
			  <%= t('.restoinfodetails')%>
		  </p>
        </div>
        <div class="col-md-6">
          <%= form_for(@restaurant, url: restaurants_path) do |f| %>
            <div class="form-group">
              <%= f.text_field :name, autofocus: true, class: 'form-control', placeholder: t('.restoname'), required: true %>
            </div>
            <div class="form-group">
              <%= f.text_area :description, class: 'form-control', placeholder: t('.description'), required: true %>
            </div>
            <div class="form-group">
              <%= f.text_area :address, class: 'form-control', placeholder: t('.restoaddress'), required: true %>
            </div>
            <p>Click the map to set up your restaurant's location</p>
            <div id="map" style="height: 400px; margin: 5px;"></div>
            <script>
              
              var map;
              var marker;

              function initMap() {
                var metromanila = {lat: 14.597537989987021, lng: 120.98281860351562};

                map = new google.maps.Map(document.getElementById('map'), {
                  zoom: 12,
                  center: metromanila
                });
                map.addListener('click', function(event) {
                  marker.setPosition(event.latLng);
                  console.log(marker.getPosition().lat())
                  console.log(marker.getPosition().lng())
                });
                addMarker(metromanila);
              }
              function addMarker(location) {
                marker = new google.maps.Marker({
                  position: location,
                  map: map
                });
              }
            </script>
            <script async defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyAmeU6jS4Tk8t3OudUKIn7XtciDfe9CbZY&callback=initMap">
            </script>
            <div class="form-group">
              <%= f.text_field :contact, class: 'form-control', placeholder: t('.contact'), required: true %>
            </div>
            <div class="actions">
              <%= f.submit t('.submitresto'), class: 'btn btn-primary', style: 'width: 100%;' %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  <% else %>
    You need to sign in first! <%= link_to 'Click Here', new_user_session_path %>
  <% end %>
</div>


