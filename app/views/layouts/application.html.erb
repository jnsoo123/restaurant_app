<!DOCTYPE html>
<html>
<head>
  <title>Fudz</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <link href="https://fonts.googleapis.com/css?family=Abel" rel="stylesheet">
  
</head>
<body>

  <div class="wrapper">
    <nav id="top-nav" class="navbar navbar-default" style="margin-bottom: 0px;">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Fudz Finder", home_path, class: 'navbar-brand' %>
        </div>

          <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to t('.home'), home_path %></li>
            <li><%= link_to "Restaurants", restaurant_search_path %></li>
            <li><a href="#cuisines"><%= t('.cuisines')%></a></li>
            <li><a href="#reviews"><%= t('.reviews')%></a></li>
            <li><a href="#make_a_resto">Make a Resto</a></li>
            <% if user_signed_in? %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" style="height: 100%;">
                  <%= icon('user', current_user.name, style: 'margin-right: 5px;') %>
                  <%= icon('chevron-down', style: 'margin-left: 5px;') %>
                </a>
                <ul id="dropdown-menu" class="dropdown-menu">
                  <% if user_signed_in? %>
                    <% if current_user.admin? %>
                      <li><%= link_to "My Admin", admin_root_path %></li>
                    <% end%>
                  <% end%>  
                  <li><%= link_to t('.myprofile'), user_profile_path(current_user.id) %></li>
                  <% if current_user.restaurants.present? %>
                    <li><%= link_to t('.myrestaurants'), users_restaurant_path %></li>
                  <% end %>
                  <li><%= link_to t('.signout'), destroy_user_session_path, method: :delete %></li>
                </ul>
              </li>
            <% else %>
              <li><%= link_to icon('user', "Login", style: 'margin-right: 8px;'), new_user_session_path %></li>
            <% end %>

          </ul>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>

    <% if success %>
      <div class="alert alert-success alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
        <%= success.html_safe %>
      </div>
    <% end %>

    <% if failure %>
      <div class="alert alert-danger alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
        <%= failure.html_safe %>
      </div>
    <% end %>

    <% if notice %>
      <div class="alert alert-success alert-dismissible" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
        <%= notice.html_safe %>
      </div>
    <% end %>
    
    <div class="container search_container">
      <div class="jumbotron" style="margin-top: 0px; background-color: whitesmoke;">

        <h1 class="text-center">Fudz Finder</h1>
        <p class="text-center" style="color: #9c9c9c;">find fud for your appetite</p>
        <div style='text-align: center;'>
          <%= form_tag(restaurant_search_path, method: :get, id: 'search_form') do %>
            <div class="input-group" style="">
              <%= text_field_tag :searchQuery, nil, class: "form-control", placeholder: "Search for fudz/cuisine/dishes...", id: 'search_field', required: true %>    
              <span class="input-group-btn">
                <%= submit_tag "Go", class: "btn btn-default"%>
                    <!-- <button class="btn btn-default" type="button">Go!</button> -->
              </span>
            </div>

          <% end %>
            <div class="row search" style="margin-top: 10px;">
              <div class="col-md-12">
                <div class="dropdown">
                  <%= link_to "Search tools#{icon('caret-down', style: 'margin-left: 10px;')}".html_safe, nil, class: 'dropdown-toggle', data: { toggle: 'dropdown'} %>
                  <ul class="dropdown-menu" style="width: 100%;">
                    <% unless Food.all.empty? %>
                      <%= form_tag(restaurant_search_path, method: :get, style: 'padding: 20px;', id: 'filter_search_form') do %>
                        <div class="form-group">
                          <%= select_tag :cuisine, options_from_collection_for_select(Cuisine.all,"id","name"), prompt: 'Select a Cuisine', class: 'form-control' %>
                        </div>
                        Filter by price range:
                        <div class="row">
                          <div class="col-md-3 col-sm-3 col-xs-3">
                            <b>PHP <%= Food.min %></b> 
                          </div>
                          <div class="col-md-6 col-sm-6 col-xs-6">
                            <input id="price_slider" name= "price_range" type="text" class="span2" value="" data-slider-min="<%= Food.min %>" data-slider-max="<%= Food.max %>" data-slider-step="5" data-slider-value="[<%= Food.min %>,<%= Food.max %>]"/> 
                          </div>
                          <div class="col-md-3 col-sm-3 col-xs-3">
                            <b class="pull-right">PHP <%= Food.max %></b>
                          </div>
                        </div>


                        <%= hidden_field_tag :searchQuery, '', id: 'filter_search_field' %>
                        <div class="actions" style="margin-top: 20px;">
                          <%= submit_tag 'Search', class: 'btn btn-default', style: 'width: 100%;', id: 'filter_search_button'%>
                        </div>
                      <% end %>
                    <% else %>
                      <h4 class="text-center" style='padding: 20px;'>No available dishes yet.</h4>
                    <% end %>
                  </ul>
                </div>
              </div>
            </div>
        </div>
      </div>
    </div>



    <%= yield %>

  </div>

<footer class="" style="background-color: #fff; height: 80px; margin-top: 30px;">
  <div class="container" style="padding-top: 25px;">
	<div class = 'row' style="margin-bottom: 0;">
		<div class = 'col-lg-9 col-md-9 col-xs-9 col-sm-9'>
			<div class = 'col-lg-2 col-md-2 col-xs-2 col-sm-2'>&copy;Soolfie</div>
			<div class = 'col-lg-2 col-md-2 col-xs-2 col-sm-2'>
				<%= link_to t('.about'), about_page_path %>
			</div>
			<div class = 'col-lg-2 col-md-2 col-xs-2 col-sm-2'>
				<%= link_to t('.contact'), contact_page_path %>
			</div>	
			<div class = 'col-lg-2 col-md-2 col-xs-2 col-sm-2'>
				<%= link_to "Get Started", new_restaurant_path %>
			</div>
		</div>
		<div class = 'col-lg-3 col-md-3 col-xs-3 col-sm-3'>
			<%= link_to "#{icon('twitter')}".html_safe, "https://www.twitter.com", style: "font-size:24px; color: #55acee; margin-right: 30px;"%>
			<%= link_to "#{icon('facebook')}".html_safe, "https://www.facebook.com", style: "font-size:24px; color: #4c66a4; margin-right: 30px;" %>
			<%= link_to "#{icon('instagram')}".html_safe, "https://www.instagram.com", style: "font-size:24px; color: #3f729b; margin-right: 30px;" %>
			<%= link_to "#{icon('youtube-play')}".html_safe, "https://www.youtube.com", style:"font-size:24px; color: #e52d27; margin-right: 30px;" %>
		</div>
	</div>  
	
 </div>
</div>
</div>
  
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
  <div class="modal-dialog modal-md" role="document">
    <div id="show-modal" class="modal-content">
      ...
    </div>
  </div>
</div>
  
   
</body>
</html>
