<!DOCTYPE html>
<html>
<head>
  <title>Fudz</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <link href="https://fonts.googleapis.com/css?family=Abel|Lobster" rel="stylesheet">
</head>
<body>
  <div id="dashboard-owner" class="container-fluid">
    <div class="row" style="">
      <div class="col-md-2" style="padding: 0">
        <nav class="navbar navbar-default" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
          <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <%= link_to t('.fudzdashboard'), users_restaurant_path, class: 'navbar-brand' %>
          </div>

          <!-- Collect the nav links, forms, and other content for toggling -->
          <div class="collapse navbar-collapse navbar-ex1-collapse">
            <ul class="nav navbar-nav">
              <li class="<%= filter_active_class(users_restaurant_path) %>
                         <% Restaurant.all.each do |x| %> <%= filter_active_class(owner_resto_edit_path(x.id)) %> <% end %>">
                <%= link_to "#{icon('cutlery', t('.myrestaurants'), style: 'margin-right: 10px;')}".html_safe, users_restaurant_path %>
              </li>
              <li class="<%= filter_active_class(owner_resto_new_path) %>"><%= link_to "#{icon('plus', t('.addrestaurants'), style: 'margin-right: 10px;')}".html_safe, 					  owner_resto_new_path %></li> 
              <li class="<%= filter_active_class(notifications_path) %>"><%= link_to "#{icon('envelope-o', "#{t('.messages')} #{current_user.check_notification}".html_safe, style: 'margin-right: 7px;')}".html_safe, notifications_path %></li>
              <li class=""><%= link_to "#{icon('reply', t('.mainpage'), 
				  	  style: 'margin-right: 7px;')}".html_safe, home_path %></li>

            </ul>
          </div><!-- /.navbar-collapse -->
        </nav>
      </div>
	  
      <div class="col-md-10" style="padding: 0;">
      <div style="padding: 15px;">
        <% if success %>
       	<div class="alert alert-success alert-dismissible" role="alert">
         	  <button type="button" class="close" data-dismiss="alert" aria-label="Close">
	   	  	 	  <span aria-hidden="true">&times;</span>
			  </button>
          <%= success.html_safe %>
        </div>
     <% end %>
		  
     <% if failure %>
        <div class="alert alert-danger alert-dismissible" role="alert">
              <button type="button" class="close" data-dismiss="alert" aria-label="Close">
				  <span aria-hidden="true">&times;</span>
			  </button>
          <%= failure.html_safe %>
        </div>
     <% end %>
		  
        <%= yield %>
      </div>
    </div>
  </div>
</div>
  
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
  <div class="modal-dialog modal-md" role="document">
    <div id="show-modal" class="modal-content">
      ...
    </div>
  </div>
</div>
  
</body>
</html>
